import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.portfolio.app',
  appName: 'ionic-portfolio-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
